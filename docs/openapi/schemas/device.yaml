RegisterDeviceRequest:
  description: "Push-notification device registration payload."
  type: "object"
  properties:
    token:
      description: "Firebase Cloud Messaging device token to register; must not be blank."
      type: "string"
      minLength: 1
  required:
    - "token"
