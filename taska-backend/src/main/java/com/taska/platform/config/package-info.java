/**
 * Typed configuration properties and the Spring wiring every feature inherits: the shared MapStruct contract, Hibernate's JSON format mapper, and
 * Firebase initialisation.
 */
@org.springframework.modulith.NamedInterface("config")
package com.taska.platform.config;
