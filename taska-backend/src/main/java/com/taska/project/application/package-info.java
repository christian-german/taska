/**
 * Project use cases.
 *
 * <p>
 * Published because the task module needs the inbox project, a project's planning calendar, and project existence — none of which it should obtain by
 * reaching for a repository.
 */
@org.springframework.modulith.NamedInterface("application")
package com.taska.project.application;
