/**
 * RRULE expansion: the instants a recurring series generates over a period.
 *
 * <p>
 * A pure function of a series and a period, exposed so that the notification sweep expands occurrences the same way the calendar does instead of
 * reimplementing it.
 */
@org.springframework.modulith.NamedInterface("recurrence")
package com.taska.task.application.recurrence;
