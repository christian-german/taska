import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';
import { FormsModule } from '@angular/forms';
import { PlanningCalendarService } from '../../core/services/planning-calendar.service';
import { PlanningCalendar, PlanningCalendarRule } from '../../core/models';
@Component({standalone:true,imports:[FormsModule],changeDetection:ChangeDetectionStrategy.OnPush,template:`<main style="padding:32px;max-width:760px"><h1 class="script">Calendriers de planification</h1><p style="color:var(--mute)">Les règles déterminent les heures autorisées pour les projets associés.</p><button class="btn btn-primary" (click)="create()">+ Nouveau calendrier</button>@for(c of calendars();track c.id){<section style="margin-top:18px;padding:16px;border:1px solid var(--line);border-radius:10px"><input [(ngModel)]="c.name" style="font-size:16px;font-weight:600"/><button class="btn btn-ghost" (click)="save(c)">Enregistrer</button><div style="margin-top:10px">@for(r of c.rules;track $index){<div style="display:flex;gap:7px;margin:5px 0;align-items:center"><select [(ngModel)]="r.dayOfWeek">@for(d of days;track d.value){<option [value]="d.value">{{d.label}}</option>}</select><input type="time" [ngModel]="toTime(r.startMinute)" (ngModelChange)="r.startMinute=toMinute($event)"/><input type="time" [ngModel]="toTime(r.endMinute)" (ngModelChange)="r.endMinute=toMinute($event)"/><button class="btn btn-ghost" (click)="remove(c,$index)">×</button></div>}<button class="btn btn-ghost" (click)="add(c)">+ règle</button></div></section>}</main>`})
export class PlanningCalendarsComponent {private api=inject(PlanningCalendarService);calendars=signal<PlanningCalendar[]>([]);days=[{value:1,label:'Lun'},{value:2,label:'Mar'},{value:3,label:'Mer'},{value:4,label:'Jeu'},{value:5,label:'Ven'},{value:6,label:'Sam'},{value:7,label:'Dim'}];constructor(){this.reload();}reload(){this.api.list().subscribe(x=>this.calendars.set(x));}create(){this.api.create({name:'Nouveau calendrier',rules:[]}).subscribe(()=>this.reload());}save(c:PlanningCalendar){this.api.update(c.id,{name:c.name,rules:c.rules}).subscribe(()=>this.reload());}add(c:PlanningCalendar){c.rules=[...c.rules,{dayOfWeek:1,startMinute:540,endMinute:1020}];}remove(c:PlanningCalendar,i:number){c.rules=c.rules.filter((_,n)=>n!==i);}toTime(m:number){return `${String(Math.floor(m/60)%24).padStart(2,'0')}:${String(m%60).padStart(2,'0')}`;}toMinute(v:string){const [h,m]=v.split(':').map(Number);return h*60+m;}}
